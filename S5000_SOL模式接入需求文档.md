# S5000 SOL 模式接入需求文档

## 1. 背景

当前 AIConfigurator 依赖系统 YAML 和算子性能数据库来估算模型在不同硬件、后端和并行配置下的推理性能。S5000 硬件已经具备基础系统规格信息，但尚未完成 GEMM、Attention、MoE、通信等算子数据库采集。

为了在算子数据库建设完成前先开展模型部署配置评估，需要将 S5000 注册到系统中，并打通 SOL（Speed-of-Light）模式。SOL 模式只基于硬件理论规格进行性能估算，不应依赖 S5000 的算子数据库表。

## 2. 目标

- S5000 能作为合法系统名被 CLI、SDK 和配置生成流程识别。
- S5000 能在 WebApp 中作为合法系统选项被选择，并通过 SOL 模式完成估算。
- 在 `--database-mode SOL` 下，S5000 不需要真实算子数据库目录或 backend version。
- 支持 S5000 在 SOL 模式下完成单点性能估算。
- 支持 S5000 在 SOL 模式下完成 `default` 模式的 agg/disagg 配置搜索。
- 对已有 H100/H200/B200/GB200 等系统的 SILICON/HYBRID 行为保持兼容。

## 3. 非目标

- 不要求补齐 S5000 的 silicon 算子数据库。
- 不要求 S5000 在默认 `SILICON` 模式下可用。
- 不要求 SOL 结果具备实测可复现性。
- 不要求对 TRT-LLM/vLLM/SGLang 生成模板做 S5000 专用改造。
- 不要求把 S5000 加入 support matrix 的 PASS 组合。

## 4. 功能需求

### 4.1 系统注册

S5000 需要注册为受支持系统：

- `--system s5000` 可以被 CLI 参数接受。
- SDK 内部 `SupportedSystems` 包含 `s5000`。
- 仓库内存在 `src/aiconfigurator/systems/s5000.yaml`，定义 S5000 的 GPU、节点和 misc 规格。
- 仓库内存在 `src/aiconfigurator/systems/data/s5000/` 占位目录，满足系统数据目录结构检查。

### 4.2 SOL 模式数据库行为

当 `database_mode=SOL` 时：

- 不调用 silicon 数据库版本解析作为必要条件。
- 未指定 `backend_version` 时，使用占位版本 `estimate`。
- 可仅基于系统 YAML 构造估算用 `PerfDatabase` 对象。
- 查询 GEMM、Attention、MoE、通信、内存等操作时走 SOL 公式。
- 缺少 `gemm_perf.txt`、`context_attention_perf.txt` 等数据文件不应导致失败。

### 4.3 CLI default 模式

以下命令应可成功执行：

```bash
python -m aiconfigurator.cli.main default \
  --model-path Qwen/Qwen3-32B \
  --total-gpus 8 \
  --system s5000 \
  --backend trtllm \
  --database-mode SOL
```

验收点：

- agg 搜索可完成。
- disagg 搜索可完成。
- 日志显示 `Using database mode: SOL`。
- 任务 backend version 显示为 `estimate`。
- 输出中包含吞吐、TTFT、TPOT、request latency 等结果。

### 4.4 CLI estimate 模式

以下命令应可成功执行：

```bash
python -m aiconfigurator.cli.main estimate \
  --model-path Qwen/Qwen3-32B \
  --system s5000 \
  --backend trtllm \
  --database-mode SOL \
  --estimate-mode agg \
  --isl 128 \
  --osl 32 \
  --batch-size 8 \
  --tp-size 1 \
  --pp-size 1
```

验收点：

- 输出 `System: s5000`。
- 输出 `Backend: trtllm (estimate)`。
- 输出 TTFT、TPOT、Request Latency、tokens/s、Memory 等指标。
- 不因缺少 S5000 算子数据文件失败。

### 4.5 SDK/API 行为

SDK 和 Python API 应满足：

- `TaskConfig(..., system_name="s5000", database_mode="SOL")` 可构造成功。
- `TaskRunner._get_database(..., database_mode="SOL")` 可用系统 YAML 构造估算数据库。
- `cli_estimate(..., system_name="s5000", database_mode="SOL")` 可执行成功。

### 4.6 WebApp 行为

WebApp 应支持 S5000 的 SOL 模式估算：

- System 下拉框包含 `s5000`。
- 选择 `s5000` 后，Backend 下拉框至少包含 `trtllm`、`sglang`、`vllm`。
- 选择 `s5000 + trtllm` 后，Version 下拉框提供占位版本 `estimate`。
- Database Mode 下拉框包含 `SOL`。
- 选择 `s5000 + trtllm + estimate + SOL` 后，可运行 Static、Agg、Agg Pareto、Disagg Pareto 等 WebApp 估算入口。
- WebApp 运行估算时复用 SDK 的 SOL 数据库加载逻辑，仅依赖系统 YAML，不要求存在 S5000 算子数据库表。
- `estimate` 版本下量化选项提供可用默认值：GEMM/KVCache/FMHA/MoE 使用 `float16`，通信使用 `half`。

### 4.7 HYBRID/SILICON 兼容

- `SILICON` 模式仍然要求真实算子数据库。
- `HYBRID` 模式在已有 silicon 数据库时继续优先使用真实数据库。
- `HYBRID` 模式在无数据库的新系统上可退化为估算路径。
- 已有系统的 latest backend version 解析结果不应被 `estimate` 误替代。

## 5. 约束

- S5000 SOL 接入应尽量复用通用 database mode 逻辑，避免在核心计算路径中到处写死 `s5000`。
- 不能破坏已有数据库加载、support matrix、CLI default、CLI estimate 的行为。
- `main` 分支应保持上游同步；S5000 相关特性放在 `mt-feature` 或后续特性分支中维护。
- SOL 模式结果只作为理论上限/早期规划参考，不应作为真实性能承诺。

## 6. 验收测试

最低验收测试如下：

```bash
uv run --extra dev pytest \
  tests/unit/cli/test_backend_any.py::TestBackendAny::test_build_default_task_configs_s5000_sol_without_database \
  tests/unit/sdk/task/test_task.py::test_taskconfig_sol_uses_estimate_version_without_database_lookup \
  tests/unit/sdk/task/test_task.py::test_taskrunner_sol_database_uses_system_yaml_without_perf_tables \
  tests/unit/webapp/test_s5000_sol.py \
  -q
```

预期结果：

```text
6 passed
```

CLI 冒烟测试：

```bash
python -m aiconfigurator.cli.main estimate \
  --model-path Qwen/Qwen3-32B \
  --system s5000 \
  --backend trtllm \
  --database-mode SOL \
  --estimate-mode agg \
  --isl 128 \
  --osl 32 \
  --batch-size 8 \
  --tp-size 1 \
  --pp-size 1
```

```bash
python -m aiconfigurator.cli.main default \
  --model-path Qwen/Qwen3-32B \
  --total-gpus 8 \
  --system s5000 \
  --backend trtllm \
  --database-mode SOL \
  --isl 128 \
  --osl 32 \
  --ttft 10000 \
  --tpot 1000 \
  --top-n 1
```

## 7. 风险与后续工作

- SOL 估算不包含 S5000 实测 kernel 行为，可能高估真实性能。
- S5000 的 empirical scaling factor 当前仍属于经验值，需要后续用实测数据校准。
- 后续完成算子数据库后，应补充 SILICON/HYBRID 模式验证。
- 若后续需要支持更多 backend/version，应补充对应生成配置和验证用例。
- 若要进入正式支持矩阵，需要补充 support matrix 数据和端到端回归结果。
