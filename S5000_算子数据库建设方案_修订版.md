# S5000 算子数据库建设方案（修订版）

## 一、背景与目标
目标是在 S5000 上建立可用于 AIC SILICON 模式的算子数据库，支撑端到端推理性能预测，包括 TTFT、TPOT 和 TGS。  
当前已经具备一定基础，AIC 框架层、模型定义层和指标拆解能力均可复用。现阶段 AIC 的核心在于以算子为最小单位，组合形成整个模型的推理流程与性能分析链路。因此，构建面向 S5000 的专属算子数据库，是提升推理性能预测精度的必要前提。  
第一阶段的目标不是一次性做全，而是先形成“粗版数据库”，优先跑通端到端预测，建立可插值、可约束、可解释的性能函数，并为后续持续补点预留机制。

## 二、算子数据库构建思路
AIC 的预测机制是算子级组合，而不是黑盒回归。端到端性能由计算算子、注意力算子、通信算子和系统开销逐层累加得到。  
如果不建设 S5000 专属库，AIC 只能使用其他 GPU 的性能表或理论值进行估算，预测误差会出现系统性偏移。因此，粗版阶段的重点不是“尽量多测”，而是优先覆盖主路径、建立基础函数形态，并保证后续可以持续细化。

## 三、粗版阶段的测试重点
粗版阶段的测试重点主要包括五类内容。  
第一类是 GEMM / GroupGEMM，需要覆盖 prefill 和 decode 两种 M 维形态。其中 prefill 场景下，`M≈BS×ISL`，对 TTFT 影响较大；decode 场景下，`M≈BS`，对 TPOT 和并发更敏感。  
第二类是 Attention（GQA），需要区分 prefill（context）和 decode（generation）两条链路。prefill 对 ISL 极为敏感，决定长上下文场景下的 TTFT；decode 对历史长度和并发敏感，决定 TPOT。  
第三类是 MoE（如适用）。MoE 场景下通常同时存在 expert compute 和 alltoall 的双重瓶颈，粗版阶段重点关注计算与通信的耦合区间。  
第四类是通信算子，包括 allreduce、allgather、reducescatter 和 alltoall。小包主要看延迟，大包主要看带宽，同时 TP / EP 的变化会直接影响消息规模与频次。  
第五类是系统校准项，包括 mem_bw scaling、constant latency、nccl_mem 和 other_mem 等参数，这些内容会直接影响端到端误差收敛效果。

## 四、补充：AIC 不同算子的关键维度与测试方法（修订重点）
你提出的问题非常关键：AIC 的数据库字段不止 ISL/OSL/BS，不同算子的关键维度不同，测试方法也必须分算子设计。  
下面给出可直接执行的“按算子建表维度”原则。

### 4.1 GEMM / GroupGEMM（计算主干）
- 核心字段：`M, N, K, dtype, quant_mode, kernel_source, tp_size`
- 与 ISL/OSL/BS 的关系：
  - prefill：`M≈BS×ISL`
  - decode：`M≈BS`
  - `N/K` 由模型结构决定（hidden/intermediate/expert hidden）
- 测试建议：
  - 固定模型结构下，重点扫 `M` 分桶（低/中/高并发）
  - 每个 `dtype/quant_mode` 单独建表，不能混用
  - GroupGEMM 需单独覆盖 `group_size` 或 expert group 相关维度

### 4.2 Attention（GQA）
- 核心字段：`num_heads, num_kv_heads, head_dim, kv_cache_dtype, batch_size, isl(or context_len), tp_size, step`
- 与 ISL/OSL/BS 的关系：
  - prefill（context attention）：对 `ISL` 高敏感，决定 TTFT
  - decode（generation attention）：对历史长度（可近似由 step/已生成长度表示）和并发敏感，决定 TPOT
- 测试建议：
  - 不能只用一个 ISL 点外推长上下文，必须做分桶（短/中/长）
  - decode 需按历史长度段采样（如 1K/4K/8K/32K）
  - kv cache dtype 与 tp_size 变化都要独立建点

### 4.3 DSA/MLA（如 DeepSeekV3.2 路径）
- 核心字段：除 batch/isl 外，还包含模块内部结构维度（如 indexer 相关维度、qk/kv lora rank、模块参数）
- 与 ISL/OSL/BS 的关系：
  - ISL/并发仍影响耗时，但不是唯一主导项
- 测试建议：
  - 必须按 architecture 对应维度建表，不能简化成纯 `ISL+BS`
  - DSA context 和 DSA generation 分开测、分开入库

### 4.4 MoE（如适用）
- 核心字段：`num_experts, top_k, tokens_per_expert(分布), expert_hidden, alltoall message size, tp/ep`
- 与 ISL/OSL/BS 的关系：
  - `BS×ISL` 决定 token 总量，影响 dispatch/combine 压力
  - decode 中并发和 step 决定持续通信负载
- 测试建议：
  - 计算和通信分开测，再联合对账
  - 覆盖典型 token 分布，而非只测均匀分布

### 4.5 通信（NCCL/custom）
- 核心字段：`collective type, message size, world size(tp/ep), topology(intra/inter node), dtype`
- 与 ISL/OSL/BS 的关系：
  - ISL/OSL/BS 不直接进表，但通过激活大小和并行策略映射到 message size/frequency
- 测试建议：
  - 小包延迟段与大包带宽段分开建模
  - allreduce/alltoall 需按 tp/ep 场景分别采样

### 4.6 系统校准项（全局参数）
- 核心字段：`mem_bw scaling, constant latency, p2p latency, nccl_mem, other_mem`
- 测试建议：
  - 用端到端误差反推校准，不建议凭经验一次性写死
  - 每轮补点后都要回归验证这些参数是否仍成立

### 4.7 粗版阶段可执行原则（避免“只测 ISL/OSL/BS”）
- 原则 1：每个算子至少覆盖“业务主维 + 2个结构维度”
- 原则 2：每个算子至少有一条 prefill 曲线和一条 decode 曲线
- 原则 3：通信与计算必须解耦采样后再联调
- 原则 4：每周产物要可插值、可回放、可复现

## 五、重排后的分周计划

### Week 1：基线与测试矩阵冻结
本周目标是把整体方案冻结到“可执行”状态，避免后续边测边改导致返工。  
主要工作包括：冻结测试对象，即 S5000、backend、backend_version 和 quant mode；确定粗版阶段的 shape 网格；统一环境配置、命令模板、产物目录和命名规范；明确数据落盘格式与后续入库规则。  
粗版 shape 网格建议保持业务覆盖优先。ISL 取值为 512、1K、2K、4K、8K、16K、32K、64K、128K；OSL 取值为 1、16、64、128、512、1K、2K、4K；并发取值为 1、2、4、8、16、32、64、128、256、512。  
本周结束时，应形成一套稳定的测试规范和测试矩阵，保证后续采集工作可以直接展开。

### Week 2：计算主干算子采集（第一阶段）
本周目标是优先建立计算主干的第一版覆盖。  
主要工作包括：完成 gemm / group_gemm 的 prefill 档位采集，优先覆盖主模型主路径；补齐 decode 档位中的高频形态；初步整理 shape 分桶方式，形成可复用的采集模板；同步进行首轮数据清洗，处理明显异常点、缺列和重复点。  
这一周的重点不是全量铺开，而是尽快把计算主干的骨架搭起来，确保后续模型在核心计算路径上不缺关键表项。

### Week 3：注意力与计算补齐（第二阶段）
本周目标是在计算主干基础上补齐注意力主路径，并完成核心算子的可用性闭环。  
主要工作包括：完成 context_attention 和 generation_attention 的粗版采集，按 ISL 和历史长度进行分桶；补齐上周未覆盖的 gemm / group_gemm 边界点；若目标模型包含 DSA（如 DeepSeekV3.2 类），同步采集 dsa_context_module 和 dsa_generation_module；开展第二轮数据质量检查，重点验证插值可用性和表项连续性。  
本周结束时，应达到“核心计算与注意力算子不缺表”的状态，确保 AIC 在 SILICON 模式下不会因主干算子缺失而失败。

### Week 4：通信算子采集（第一阶段）
本周目标是建立通信侧的基础性能曲线。  
主要工作包括：采集 allreduce、allgather、reducescatter 和 alltoall 的基础数据；按消息大小分桶，区分小包延迟区间和大包带宽区间；结合 TP / EP 配置变化，补充典型消息规模和频次对应点；对原始测试结果做初步整理，识别异常波动和不可复现点。  
这一周先把通信主轮廓测出来，不追求一步到位，但要能支撑模型通信开销从“未知项”变成“有基础可估项”。

### Week 5：通信补齐、MoE 与系统参数校准（第二阶段）
本周目标是补齐通信误差大头，并完成系统侧粗校准。  
主要工作包括：补测通信边界点和高误差区间；若模型包含 MoE，则完成 dispatch / combine 和 expert compute 的粗版覆盖；结合通信与计算实测结果，校准 mem_bw scaling、constant latency、nccl_mem 和 other_mem；对比理论峰值与实测峰值，记录偏差来源，并沉淀到参数建议中。  
本周结束时，通信与 MoE 不再作为端到端预测中的未知项，系统参数也应具备首版可用校准值。

### Week 6：端到端回放与误差闭环
本周目标是完成粗版数据库的端到端验证，形成可用预测版本。  
主要工作包括：使用主 workload 矩阵做 E2E 对账，覆盖 TTFT、TPOT 和 TGS；输出误差分布，重点关注 P50、P90 和 Top 偏差 case；针对误差较大的 case 反推短板算子，并优先补测 SLO 边界附近的并发点；最终形成数据库 v0.9 的发布候选版本。  
本周结束时，粗版数据库应达到“可用预测”标准，即主场景基本可解释，误差具备收敛路径，后续补点方向明确。

## 六、结论
本方案修订后，测试设计从“只看 ISL/OSL/BS”升级为“按算子关键维度建表 + 分阶段闭环校准”。  
这样可以在粗版阶段快速获得可用预测能力，并在后续以误差驱动持续提升 S5000 端到端仿真精度。
